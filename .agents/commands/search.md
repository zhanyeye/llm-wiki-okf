---
description: 全文搜索知识库（llm-wiki search）
---

调用 llm-wiki skill 的 **Search** 操作：读 `.agents/skills/llm-wiki/SKILL.md` 中 `### 5. Search (search)`。在 `wiki/` 内全文检索，给出带上下文的命中，并提议打开相关页；结束后在 `wiki/log.md` 记一行。

用户的检索词：$ARGUMENTS

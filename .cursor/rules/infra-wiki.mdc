---
description: 按意图使用 infra-wiki Skill；查询走 query，写入走 ingest/types。
alwaysApply: true
---

处理本仓库的运维知识、值班排查、文档入库或知识库体检时，使用项目 Skill `infra-wiki`：

- **查 / 排障 / 东西在哪**：只跟 `query.md`。先读根 `index.md`，再打开相关分组 index 与 2–5 篇正文。不要通读 `AGENTS.md` 全文，不要读 `raw/`，不要一次 Glob/打开整库。
- **入库 / 迁文档 / 故障结案 / 从零写页**：读 `ingest.md` 或 `author.md`，并读 `types.md` + `index-log.md`。
- **体检**：读 `lint.md`。
